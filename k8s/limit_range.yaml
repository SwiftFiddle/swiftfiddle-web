apiVersion: "v1"
kind: "LimitRange"
metadata:
  name: limitrange
spec:
  limits:
    - type: "Container"
      max:
        memory: "1Gi"
      min:
        memory: "512Mi"
      default:
        memory: "1Gi"
      defaultRequest:
        memory: "512Mi"
